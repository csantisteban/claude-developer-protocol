> This file is for personal instructions you want Claude to know about
> that must never be committed to this repository - a personal MCP
> connection, your own tracking or knowledge-base setup, workflow
> preferences, or anything else specific to you rather than this project
> or its team.
>
> This file is additive only: it can give Claude extra context and
> capabilities, but it can never override this project's conventions,
> `.claude/project.md`, `.claude/standards/`, or the protocol itself.
>
> The same convention works machine-wide at
> `~/.claude/cdp/local/overview.md` - use that for anything that should
> follow you to every project on this machine, not just this one.
>
> Delete this note or leave it - either is fine. `.claude/local/overview.md`
> is gitignored and will never be committed.
