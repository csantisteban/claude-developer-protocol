# Test Case {nnn}: {Title}

**Verifies:** {free text - requirement(s), task(s), or behavior covered}
**Requires:** {Preconditions - migrations deployed, prior test passed, etc.}
**Status:** {Not run / Passed - YYYY-MM-DD / Failed - YYYY-MM-DD}

## Trigger

{What kicks off this test - an HTTP call, a script, a manual action, or "none -
query state directly"}

## Steps

1. {Step description}
   - Method: {SQL / HTTP / Manual / etc.}
   - Detail:
     ```
     {query, request, or instructions}
     ```
   - Expected: {exact expected result}
